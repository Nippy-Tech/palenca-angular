export enum OnEvent {
  ready = 'ready',
  connection_success = 'connection_success',
  connection_error = 'connection_error',
  user_created = 'user_created',
}

export enum WidgetEventSignal {
  ready = 'ready',
  connection_success = 'connection_success',
  connection_error = 'connection_error',
  user_created = 'user_created',
  close = 'close',
}
